{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{{ attach_library('andres/full-horizontal-card') }}
{% set tag = '' %}
{% set tag_style = '' %}
{% set body = '' %}
{% set header = '' %}
{% set pretitle = '' %}
{% set main_cta = '' %}
{% set main_cta_label = '' %}
{% set secondary_cta = '' %}
{% set secondary_cta_label = '' %}
{% set image_background_src = '' %}
{% set theme = '' %}
{% set tags = paragraph.field_tag %}
{% set reverse = '' %}

{% if paragraph.field_body.value %}
	{% set body = paragraph.field_body.value|raw %}
{% endif %}
{% if paragraph.field_rich_header.value %}
	{% set header = paragraph.field_rich_header.value|raw %}
{% endif %}
{% if paragraph.field_pretitle.value %}
	{% set pretitle = paragraph.field_pretitle.value|raw %}
{% endif %}
{% if paragraph.field_main_cta.uri %}
	{% set main_cta = paragraph.field_main_cta.uri %}
{% endif %}
{% if paragraph.field_main_cta_title.value %}
	{% set main_cta_label = paragraph.field_main_cta_title.value|raw %}
{% endif %}
{% if paragraph.field_secondary_cta.uri %}
	{% set secondary_cta = paragraph.field_secondary_cta.uri %}
{% endif %}
{% if paragraph.field_secondary_cta_title.value %}
	{% set secondary_cta_label = paragraph.field_secondary_cta_title.value|raw %}
{% endif %}
{% if paragraph.field_main_image.entity.field_media_image.entity.uri.value %}
	{% set image_background_src = file_url(paragraph.field_main_image.entity.field_media_image.entity.uri.value) %}
{% endif %}
{% if paragraph.field_theme.value %}
	{% set theme = paragraph.field_theme.value %}
{% endif %}
{% if paragraph.field_reverse.value == 1 %}
	{% set reverse = 'reverse' %}
{% endif %}

{%
  set classes = [
    reverse,
    'full-horizontal-card',
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}

{% block paragraph %}
	<div{{attributes.addClass(classes)}}>
		<div class="full-horizontal-card-card-img-wrapper">
      <img src="{{ image_background_src }}" alt="{{ pretitle }}" class="full-horizontal-card-img">
		</div>
		<div class="full-horizontal-card-content">
			<header>
				<span class="full-horizontal-card-category text-code-style text-code-{{ theme }}">{{ pretitle }}</span>
				<h2 class="full-horizontal-card-title">{{ header|raw }}</h2>
			</header>
			<div class="full-horizontal-card-description">
				{{ body|raw }}
			</div>
			<footer class="full-horizontal-card-tags">
				{% for tag in tags %}
          <div class="tag tag-{{ theme }}">
            {{ tag.value }}
          </div>
        {% endfor %}
			</footer>
		</div>
	</div>
{% endblock paragraph %}
